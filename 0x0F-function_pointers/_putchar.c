#include <stdio.h>

/**
 * _putchar - prints a single character to screen
 * @value: the character in base 10)
 *
 * Return: int (ascii equ of character)
 *
 */

int _putchar(int value)
{
	printf("%c", value);
	return (value);
}
