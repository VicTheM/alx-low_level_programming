#include <stdio.h>
/**
 * main - prints the number of command line args
 * @argc: nof command line arguments
 * @argv: unused variable
 *
 * Return: 0 Success
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
