#include "main.h"

/**
 * mul - checks if a character is upper
 * @a: first operand
 * @b: second operand
 * Description: prints using a UDF
 *
 * Return: Always 1 (true) 0 (false)
 */
int mul(int a, int b)
{
	return (a * b);
}
