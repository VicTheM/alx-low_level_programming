#include <stdio.h>

/**
  * main - entry point
  *
  * return: always
  */

int main(void)
{
	int i;
        printf("Infinite loop incoming :(\n");

        i = 0;

        /**
	 * while (i < 1)
	 * {
	 *	putchar(i);
         */

        printf("Infinite loop avoided! \\o/\n");

        return (0);
}
